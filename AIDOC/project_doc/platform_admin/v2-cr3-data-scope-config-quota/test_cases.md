# 测试用例：数据权限增强 + 三级配置 + 配额管理（V2-CR3）

**关联文档**: requirements.md / design.md / tasks.md
**编写日期**: 2026-07-21
**测试类型**: 功能测试 / 接口测试 / 回归测试 / 数据验证
**测试方法**: 等价类划分、边界值分析、状态转换测试、错误推测、场景法

---

## 测试矩阵概览

| 模块 | 正向 | 反向 | 边界 | 回归 | 接口 | 数据验证 | 合计 |
|------|------|------|------|------|------|----------|------|
| DataScope scope_type | 6 | 2 | 2 | 2 | 0 | 1 | 13 |
| 组织架构 | 3 | 3 | 2 | 0 | 7 | 1 | 16 |
| 三级配置 | 4 | 3 | 2 | 1 | 8 | 3 | 21 |
| 功能开关 | 2 | 1 | 0 | 1 | 0 | 0 | 4 |
| 配额管理 | 2 | 3 | 2 | 1 | 0 | 1 | 9 |
| 前端功能 | 3 | 0 | 0 | 0 | 0 | 0 | 3 |
| **总计** | **20** | **12** | **8** | **5** | **15** | **6** | **66** |

---

## 一、正向测试（Happy Path）

### TC-001: scope_type=ALL 不注入 WHERE 条件

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 角色 A 对维度 org_id 配置 scope_type=ALL，用户 U1 拥有角色 A |
| **测试步骤** | 1. U1 查询业务表数据 |
| **预期结果** | 返回全部数据，SQL 中无 org_id 相关 WHERE 条件 |
| **关联需求** | FR-1 |
| **设计方法** | 等价类划分 |

### TC-002: scope_type=SELF 注入 create_by 过滤

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 角色 B 对维度 x 配置 scope_type=SELF，用户 U2(ID=100) 拥有角色 B，表中有 U2 创建和他人创建的数据 |
| **测试步骤** | 1. U2 查询业务表数据 |
| **预期结果** | 仅返回 create_by=100 的记录 |
| **关联需求** | FR-1 |
| **设计方法** | 等价类划分 |

### TC-003: scope_type=DEPT 注入本组织节点过滤

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | U3 主归属组织节点 OrgA(ID=10)，角色配置 scope_type=DEPT column=dept_id，表中有 dept_id=10 和 dept_id=20 的数据 |
| **测试步骤** | 1. U3 查询业务表 |
| **预期结果** | 仅返回 dept_id=10 的记录 |
| **关联需求** | FR-1 |
| **设计方法** | 等价类划分 |

### TC-004: scope_type=DEPT_TREE 注入本组织及下级过滤

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | U4 主归属 OrgB(ID=10)，OrgB 有子节点 OrgC(ID=11)、OrgD(ID=12)，角色配置 scope_type=DEPT_TREE |
| **测试步骤** | 1. U4 查询业务表 |
| **预期结果** | 返回 dept_id IN (10, 11, 12) 的记录 |
| **关联需求** | FR-1 |
| **设计方法** | 场景法 |

### TC-005: scope_type=CUSTOM 保持现有值列表过滤

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 角色配置 scope_type=CUSTOM，dimension_values=["v1","v2"] |
| **测试步骤** | 1. 用户查询业务表 |
| **预期结果** | 返回 column IN ('v1', 'v2') 的记录 |
| **关联需求** | FR-1 |
| **设计方法** | 等价类划分 |

### TC-006: 多角色 ALL 短路放行

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | U5 有角色 R1(scope_type=SELF) 和 R2(scope_type=ALL) 同一维度 |
| **测试步骤** | 1. U5 查询业务表 |
| **预期结果** | ALL 短路，返回全部数据 |
| **关联需求** | FR-1 |
| **设计方法** | 判定表 |

### TC-007: 三级配置 Resolve 优先级 USER > TENANT > SYSTEM

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | key="app.theme", SYSTEM 值="light", TENANT 值="dark", USER 值="blue" |
| **测试步骤** | 1. 调用 Resolve("app.theme", tenantID, userID) |
| **预期结果** | 返回 "blue" |
| **关联需求** | FR-4 |
| **设计方法** | 场景法 |

### TC-008: 仅 SYSTEM scope 有值时返回 SYSTEM 值

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | key="app.lang", 仅 SYSTEM 值="zh-CN" |
| **测试步骤** | 1. 调用 Resolve("app.lang", tenantID, userID) |
| **预期结果** | 返回 "zh-CN" |
| **关联需求** | FR-4 |
| **设计方法** | 等价类划分 |

### TC-009: 组织架构树形展示

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 租户下有组织树：公司 > 技术部 > 前端组 |
| **测试步骤** | 1. GET /api/v1/admin/org-units/tree |
| **预期结果** | 返回嵌套结构，children 正确层级 |
| **关联需求** | FR-2 |
| **设计方法** | 场景法 |

### TC-010: 创建组织节点

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 已有顶级公司节点 |
| **测试步骤** | 1. POST /api/v1/admin/org-units body: {parent_id, node_type: "DEPARTMENT", name: "市场部"} |
| **预期结果** | HTTP 200, 返回新节点含 id/version=1 |
| **关联需求** | FR-2 |
| **设计方法** | 等价类划分 |

### TC-011: 节点用户分配

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 节点 A 存在，用户 U1/U2 存在 |
| **测试步骤** | 1. PUT /api/v1/admin/org-units/:id/users body: {user_ids: ["u1","u2"], is_primary: 1} |
| **预期结果** | admin_user_org 表新增 2 条记录 |
| **关联需求** | FR-2 |
| **设计方法** | 场景法 |

### TC-012: 配额未超限时正常创建

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 租户当前用户数=5, quota.max_admin_users=50 |
| **测试步骤** | 1. 创建新用户 |
| **预期结果** | 创建成功 |
| **关联需求** | FR-8 |
| **设计方法** | 等价类划分 |

### TC-013: SUPER_ADMIN 跳过配额

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 租户当前用户数=50, quota.max_admin_users=50, 操作者为 SUPER_ADMIN |
| **测试步骤** | 1. SUPER_ADMIN 创建新用户 |
| **预期结果** | 创建成功（不受配额限制） |
| **关联需求** | FR-8 |
| **设计方法** | 场景法 |

### TC-014: 功能开关开启时正常访问

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | feature.user-mgmt.enabled="true" |
| **测试步骤** | 1. 访问 /api/v1/admin/users |
| **预期结果** | 正常返回数据（不被拦截） |
| **关联需求** | FR-7 |
| **设计方法** | 等价类划分 |

### TC-015: 功能开关未配置时默认开启

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | admin_config 中无 feature.xxx.enabled 记录 |
| **测试步骤** | 1. 访问对应模块 API |
| **预期结果** | 正常返回（默认开启） |
| **关联需求** | FR-7 |
| **设计方法** | 错误推测 |

### TC-016: sys_config 迁移后数据完整

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | sys_config 有 3 条记录 |
| **测试步骤** | 1. 启动系统（触发迁移） 2. 查询 admin_config |
| **预期结果** | admin_config 含对应 3 条 scope=SYSTEM 记录，字段映射正确 |
| **关联需求** | FR-5 |
| **设计方法** | 场景法 |

### TC-017: 配置缓存命中（单元测试级别）

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **测试级别** | 单元测试（非接口黑盒） |
| **前置条件** | 首次调用 Resolve 后缓存已写入 |
| **测试步骤** | 1. 调用 Resolve 同一 key 两次 2. Mock DB 层验证第二次未产生查询（或验证响应时间 < 5ms） |
| **预期结果** | 第二次调用不产生 DB 查询 |
| **关联需求** | FR-4 |
| **设计方法** | 场景法 |

---

## 二、反向测试（Negative）

### TC-N01: OrganizationProvider 返回空切片时无数据

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 用户未关联任何组织节点，角色配置 scope_type=DEPT |
| **测试步骤** | 1. 用户查询业务表 |
| **预期结果** | 返回空列表（注入 1=0） |
| **关联需求** | FR-1 |
| **设计方法** | 错误推测 |

### TC-N02: 删除有子节点的组织报错

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 节点 A 有子节点 B |
| **测试步骤** | 1. DELETE /api/v1/admin/org-units/:A_id |
| **预期结果** | HTTP 400, message="该节点下存在子节点，无法删除" |
| **关联需求** | FR-2 |
| **设计方法** | 错误推测 |

### TC-N03: 组织编码重复报错

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 已有 code="tech" 的节点 |
| **测试步骤** | 1. POST /api/v1/admin/org-units body: {code: "tech", ...} |
| **预期结果** | HTTP 400, message="组织编码已存在" |
| **关联需求** | FR-2 |
| **设计方法** | 等价类划分 |

### TC-N04: 配额超限拒绝创建用户

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 租户当前用户数=50, quota.max_admin_users=50, 操作者非 SUPER_ADMIN |
| **测试步骤** | 1. 创建新用户 |
| **预期结果** | HTTP 400, code=40011, message="管理用户数已达上限" |
| **关联需求** | FR-8 |
| **设计方法** | 边界值分析 |

### TC-N05: 配额超限拒绝创建角色

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 租户当前角色数=20, quota.max_roles=20 |
| **测试步骤** | 1. 创建新角色 |
| **预期结果** | HTTP 400, code=40011, message="角色数已达上限" |
| **关联需求** | FR-8 |
| **设计方法** | 边界值分析 |

### TC-N06: 配额超限拒绝订阅应用

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 租户当前应用数=5, quota.max_apps=5 |
| **测试步骤** | 1. 订阅新应用 |
| **预期结果** | HTTP 400, code=40011, message="可订阅应用数已达上限" |
| **关联需求** | FR-8 |
| **设计方法** | 边界值分析 |

### TC-N07: 功能开关关闭时返回 40302

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | feature.user-mgmt.enabled="false" |
| **测试步骤** | 1. 非 SUPER_ADMIN 访问 /api/v1/admin/users |
| **预期结果** | HTTP 403, code=40302, message="该功能未开启" |
| **关联需求** | FR-7 |
| **设计方法** | 状态转换测试 |

### TC-N08: scope_type 不在 supported_scope_types 中

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 维度 X 的 supported_scope_types=["ALL","SELF","CUSTOM"] |
| **测试步骤** | 1. 角色配置该维度 scope_type="DEPT" |
| **预期结果** | 拒绝配置，返回错误提示 |
| **关联需求** | FR-3 |
| **设计方法** | 等价类划分 |

### TC-N09: 乐观锁冲突

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 组织节点 version=1 |
| **测试步骤** | 1. PUT /api/v1/admin/org-units/:id body: {version: 2, name: "新名"} |
| **预期结果** | HTTP 400, message="数据已被其他操作修改，请刷新后重试" |
| **关联需求** | design.md RG |
| **设计方法** | 错误推测 |

### TC-N10: 三级配置所有 scope 无值时返回 defaultValue

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | admin_config 中无 key="nonexist" 的任何记录 |
| **测试步骤** | 1. 调用 ResolveInt(tenantID, "nonexist", 42) |
| **预期结果** | 返回 42 |
| **关联需求** | FR-4 |
| **设计方法** | 等价类划分 |

### TC-N11: TENANT scope 写入校验 tenant_id 归属（待实现）

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 当前用户 tenant_id=1 |
| **测试步骤** | 1. POST /api/v1/admin/configs body: {scope: "TENANT", tenant_id: "999", ...} |
| **预期结果** | 拒绝（tenant_id 不属于当前用户） |
| **关联需求** | FR-4 |
| **设计方法** | 错误推测 |
| **备注** | ⚠️ 当前 AdminConfigService.Create 未实现归属校验，此用例为「待实现功能验证」，首次执行预期 FAIL |

---

## 三、边界测试（Boundary）

### TC-B01: 配额恰好等于上限时拒绝

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **测试数据** | 当前用户数=50, quota=50 |
| **预期结果** | 创建被拒绝（>=50 时拒绝） |
| **关联需求** | FR-8 |
| **设计方法** | 边界值分析 |

### TC-B02: 配额恰好未达上限时允许

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **测试数据** | 当前用户数=49, quota=50 |
| **预期结果** | 创建成功 |
| **关联需求** | FR-8 |
| **设计方法** | 边界值分析 |

### TC-B03: 组织架构树最大深度

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **测试数据** | 创建 10 层嵌套组织节点 |
| **预期结果** | GetSubOrgIds 正确返回所有 10 层节点 ID |
| **关联需求** | FR-2 |
| **设计方法** | 边界值分析 |

### TC-B04: 缓存 TTL 过期后重新查库

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **测试数据** | 缓存 TTL=60s |
| **预期结果** | 60s 后再次调用 Resolve，产生新的 DB 查询 |
| **关联需求** | FR-4 |
| **设计方法** | 边界值分析 |

### TC-B05: config_key 最大长度 128 字符

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **测试数据** | key 长度=128 → 成功；key 长度=129 → 拒绝 |
| **预期结果** | 128 字符正常创建，129 字符被数据库约束拒绝 |
| **关联需求** | FR-6 |
| **设计方法** | 边界值分析 |

### TC-B06: 多角色同维度条件叠加

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **测试数据** | 角色 R1=SELF, 角色 R2=DEPT(org_id=10)，用户 create_by=100 且 dept_id 不是 10 |
| **预期结果** | 当前实现为逐条 Where 叠加。若需求为并集（OR），需确认实现已支持 OR 逻辑；若为交集（AND），则返回同时满足 create_by=100 AND dept_id=10 的数据（可能为空）。**注：requirements.md 定义为取并集（最宽松），实现层需用 OR 包裹多角色条件** |
| **关联需求** | FR-1 |
| **设计方法** | 判定表 |

### TC-B07: 空组织架构时 DEPT/DEPT_TREE 返回空结果

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **测试数据** | admin_org_unit 表无数据 |
| **预期结果** | 查询返回空列表，不报错 |
| **关联需求** | FR-2 |
| **设计方法** | 边界值分析 |

---

## 四、回归测试（Regression）

### TC-R01: 现有 CUSTOM 维度行为不变

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **关联** | RG-1 |
| **验证步骤** | 1. 配置 scope_type=CUSTOM + dimension_values=["a","b"] 2. 查询业务表 |
| **预期结果** | 返回 column IN ('a','b') 的记录，与 CR-3 前行为完全一致 |
| **设计方法** | 回归验证 |

### TC-R02: 记录共享逻辑不受 scope_type 影响

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **关联** | RG-2 |
| **验证步骤** | 1. 配置数据权限 scope_type=SELF 2. 为某条其他用户创建的记录添加共享给当前用户 3. 查询 |
| **预期结果** | 返回自己创建的 + 被共享的记录 |
| **设计方法** | 回归验证 |

### TC-R03: 现有配置接口兼容

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **关联** | RG-3 |
| **验证步骤** | 1. GET /api/v1/admin/configs（迁移后） |
| **预期结果** | 返回配置列表，包含从 sys_config 迁移来的数据 |
| **设计方法** | 回归验证 |

### TC-R04: SUPER_ADMIN 不受配额限制

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **关联** | RG-4 |
| **验证步骤** | 1. SUPER_ADMIN 在配额已满时创建用户 |
| **预期结果** | 创建成功 |
| **设计方法** | 回归验证 |

### TC-R05: 免检接口不受功能开关影响

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **关联** | RG-5 |
| **验证步骤** | 1. 关闭所有功能开关 2. POST /auth/login |
| **预期结果** | 登录正常，不返回 40302 |
| **设计方法** | 回归验证 |

---

## 五、接口测试（API Level）

### TC-A01: GET /api/v1/admin/org-units/tree — 正向

| 字段 | 内容 |
|------|------|
| **请求** | `GET /api/v1/admin/org-units/tree` |
| **Headers** | Authorization: Bearer {access_token} |
| **预期响应** | HTTP 200, code=0, data=[{id, name, children}...] |
| **关联需求** | FR-2 |

### TC-A02: GET /api/v1/admin/org-units/tree — 未认证

| 字段 | 内容 |
|------|------|
| **请求** | `GET /api/v1/admin/org-units/tree`（无 token） |
| **预期响应** | HTTP 401, code=40101 |

### TC-A03: POST /api/v1/admin/org-units — 正向

| 字段 | 内容 |
|------|------|
| **请求** | `POST /api/v1/admin/org-units` |
| **Body** | `{"node_type":"DEPARTMENT","name":"研发部"}` |
| **预期响应** | HTTP 200, code=0, data.id 非空 |
| **关联需求** | FR-2 |

### TC-A04: POST /api/v1/admin/org-units — 缺少必填字段

| 字段 | 内容 |
|------|------|
| **请求** | `POST /api/v1/admin/org-units` |
| **Body** | `{"code":"rnd"}` （缺 node_type 和 name） |
| **预期响应** | HTTP 400, code=40000 |

### TC-A05: GET /api/v1/admin/configs/resolve/:key — 正向

| 字段 | 内容 |
|------|------|
| **请求** | `GET /api/v1/admin/configs/resolve/app.theme` |
| **预期响应** | HTTP 200, code=0, data.value="xxx", data.found=true |
| **关联需求** | FR-6 |

### TC-A06: GET /api/v1/admin/configs/resolve/:key — key 不存在

| 字段 | 内容 |
|------|------|
| **请求** | `GET /api/v1/admin/configs/resolve/nonexist.key` |
| **预期响应** | HTTP 200, code=0, data.found=false |
| **关联需求** | FR-6 |

### TC-A07: GET /api/v1/admin/configs/feature-flags — 正向

| 字段 | 内容 |
|------|------|
| **请求** | `GET /api/v1/admin/configs/feature-flags` |
| **预期响应** | HTTP 200, code=0, data=[{key, display_name, enabled}...] |
| **关联需求** | FR-6 |

### TC-A08: POST /api/v1/admin/configs — 创建 SYSTEM 配置

| 字段 | 内容 |
|------|------|
| **请求** | `POST /api/v1/admin/configs` |
| **Body** | `{"config_key":"test.key","config_value":"val","scope":"SYSTEM","scope_id":"0","tenant_id":"0"}` |
| **预期响应** | HTTP 200, code=0 |
| **关联需求** | FR-6 |

### TC-A09: DELETE /api/v1/admin/configs/:id — 正向

| 字段 | 内容 |
|------|------|
| **请求** | `DELETE /api/v1/admin/configs/{id}` |
| **预期响应** | HTTP 200, code=0 |
| **关联需求** | FR-6 |

### TC-A10: DELETE /api/v1/admin/configs/:id — 无权限

| 字段 | 内容 |
|------|------|
| **请求** | `DELETE /api/v1/admin/configs/{id}`（普通用户无 config:delete 权限） |
| **预期响应** | HTTP 403, code=40301 |

### TC-A11: PUT /api/v1/admin/org-units/:id — 乐观锁 version 正确

| 字段 | 内容 |
|------|------|
| **请求** | `PUT /api/v1/admin/org-units/{id}` |
| **Body** | `{"name":"新名称","version":1}` |
| **预期响应** | HTTP 200, code=0, data.version=2 |
| **关联需求** | FR-2 |

### TC-A12: GET /api/v1/admin/configs — 按 scope 过滤

| 字段 | 内容 |
|------|------|
| **请求** | `GET /api/v1/admin/configs?scope=TENANT&page=1&page_size=20` |
| **预期响应** | HTTP 200, list 中所有记录 scope="TENANT" |
| **关联需求** | FR-6 |

### TC-A13: GET /api/v1/admin/configs — 按 is_feature_flag 过滤

| 字段 | 内容 |
|------|------|
| **请求** | `GET /api/v1/admin/configs?is_feature_flag=1` |
| **预期响应** | HTTP 200, list 中所有记录 is_feature_flag=1 |
| **关联需求** | FR-6 |

### TC-A14: PUT /api/v1/admin/org-units/:id/users — 正向

| 字段 | 内容 |
|------|------|
| **请求** | `PUT /api/v1/admin/org-units/{id}/users` |
| **Body** | `{"user_ids":["1","2"],"is_primary":1}` |
| **预期响应** | HTTP 200, code=0 |
| **关联需求** | FR-2 |

### TC-A15: DELETE /api/v1/admin/org-units/:id — 无权限

| 字段 | 内容 |
|------|------|
| **请求** | `DELETE /api/v1/admin/org-units/{id}`（普通用户无 org:delete 权限） |
| **预期响应** | HTTP 403, code=40301 |

---

## 六、数据验证

### TC-D01: sys_config 迁移后数据完整性

| 字段 | 内容 |
|------|------|
| **触发操作** | 系统启动迁移 |
| **验证 SQL** | `SELECT COUNT(*) FROM admin_config WHERE scope='SYSTEM' AND scope_id=0 AND tenant_id=0` |
| **预期结果** | 数量 >= sys_config 原始记录数 |

### TC-D02: 组织节点用户关联正确

| 字段 | 内容 |
|------|------|
| **触发操作** | 设置节点用户 |
| **验证 SQL** | `SELECT * FROM admin_user_org WHERE org_unit_id=? AND tenant_id=?` |
| **预期结果** | 记录数等于分配的用户数，is_primary 值正确 |

### TC-D03: admin_data_scope 旧数据 scope_type 默认值

| 字段 | 内容 |
|------|------|
| **触发操作** | DDL 迁移后 |
| **验证 SQL** | `SELECT scope_type FROM admin_data_scope WHERE scope_type IS NULL OR scope_type = ''` |
| **预期结果** | 0 条（全部为 'CUSTOM'） |

### TC-D04: 配置缓存失效后数据一致

| 字段 | 内容 |
|------|------|
| **触发操作** | 更新 admin_config 某条记录 → InvalidateCache → 再次 Resolve |
| **验证 SQL** | `SELECT config_value FROM admin_config WHERE config_key=? ORDER BY ...` |
| **预期结果** | Resolve 返回值 === 数据库最新值 |

### TC-D05: 配额 seed 数据存在

| 字段 | 内容 |
|------|------|
| **触发操作** | 系统启动 |
| **验证 SQL** | `SELECT * FROM admin_config WHERE config_key LIKE 'quota.%' AND scope='SYSTEM'` |
| **预期结果** | 含 quota.max_admin_users/quota.max_roles/quota.max_apps 三条，值均为 "999999" |

### TC-D06: 三级配置唯一约束

| 字段 | 内容 |
|------|------|
| **触发操作** | 尝试插入重复 (scope, scope_id, tenant_id, config_key) |
| **验证** | 数据库返回唯一约束错误 |
| **预期结果** | 插入失败，不产生重复记录 |

---

## 七、前端功能验证（手工/E2E）

### TC-F01: 三级配置页 Tab 切换

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 管理员登录后进入系统配置页 |
| **测试步骤** | 1. 点击"系统配置" Tab 2. 点击"租户配置" Tab 3. 点击"用户配置" Tab |
| **预期结果** | 每次切换后列表刷新，显示对应 scope 的配置项 |
| **关联需求** | FR-9 |
| **设计方法** | 场景法 |

### TC-F02: USER scope 配置创建时用户选择器

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 切换到"用户配置" Tab，点击"新增配置" |
| **测试步骤** | 1. Scope 选择 USER 2. 观察表单 |
| **预期结果** | 出现"目标用户"下拉搜索框，可搜索当前租户下的用户 |
| **关联需求** | FR-9 |
| **设计方法** | 场景法 |

### TC-F03: 角色数据权限 scope_type 下拉选择

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 进入角色详情 → 数据权限 Tab |
| **测试步骤** | 1. 看到维度列表含"范围类型"列 2. 下拉选择 DEPT_TREE 3. 观察"值"列变化 |
| **预期结果** | 选择非 CUSTOM 时值列变为只读提示标签（如"本组织及下级节点数据"），不展示值输入框 |
| **关联需求** | FR-10 |
| **设计方法** | 状态转换测试 |

---

## 执行结果记录（测试执行时填写）

| 用例编号 | 结果 | 执行人 | 日期 | 备注 |
|----------|------|--------|------|------|
| TC-001 | ⬜ | | | |
| TC-002 | ⬜ | | | |
| TC-003 | ⬜ | | | |
| TC-004 | ⬜ | | | |
| TC-005 | ⬜ | | | |
| TC-006 | ⬜ | | | |
| TC-007 | ⬜ | | | |
| TC-008 | ⬜ | | | |
| TC-009 | ⬜ | | | |
| TC-010 | ⬜ | | | |
| TC-011 | ⬜ | | | |
| TC-012 | ⬜ | | | |
| TC-013 | ⬜ | | | |
| TC-014 | ⬜ | | | |
| TC-015 | ⬜ | | | |
| TC-016 | ⬜ | | | |
| TC-017 | ⬜ | | | |
| TC-N01 | ⬜ | | | |
| TC-N02 | ⬜ | | | |
| TC-N03 | ⬜ | | | |
| TC-N04 | ⬜ | | | |
| TC-N05 | ⬜ | | | |
| TC-N06 | ⬜ | | | |
| TC-N07 | ⬜ | | | |
| TC-N08 | ⬜ | | | |
| TC-N09 | ⬜ | | | |
| TC-N10 | ⬜ | | | |
| TC-N11 | ⬜ | | | |
| TC-B01 | ⬜ | | | |
| TC-B02 | ⬜ | | | |
| TC-B03 | ⬜ | | | |
| TC-B04 | ⬜ | | | |
| TC-B05 | ⬜ | | | |
| TC-B06 | ⬜ | | | |
| TC-B07 | ⬜ | | | |
| TC-R01 | ⬜ | | | |
| TC-R02 | ⬜ | | | |
| TC-R03 | ⬜ | | | |
| TC-R04 | ⬜ | | | |
| TC-R05 | ⬜ | | | |
| TC-A01 | ⬜ | | | |
| TC-A02 | ⬜ | | | |
| TC-A03 | ⬜ | | | |
| TC-A04 | ⬜ | | | |
| TC-A05 | ⬜ | | | |
| TC-A06 | ⬜ | | | |
| TC-A07 | ⬜ | | | |
| TC-A08 | ⬜ | | | |
| TC-A09 | ⬜ | | | |
| TC-A10 | ⬜ | | | |
| TC-A11 | ⬜ | | | |
| TC-A12 | ⬜ | | | |
| TC-A13 | ⬜ | | | |
| TC-A14 | ⬜ | | | |
| TC-A15 | ⬜ | | | |
| TC-D01 | ⬜ | | | |
| TC-D02 | ⬜ | | | |
| TC-D03 | ⬜ | | | |
| TC-D04 | ⬜ | | | |
| TC-D05 | ⬜ | | | |
| TC-D06 | ⬜ | | | |
