# 需求计划：DLC 管理功能完善

## 背景

当前 DLC 管理只有基础 CRUD，缺少文件上传/下载、游戏关联选择、版本管理等实用功能。

## 澄清问题

[Question] Q1: PCK 文件上传后存储在本地磁盘还是对象存储（如 S3/OSS）？当前后端写死 `dlc_files/` 本地目录。
[Answer]
暂时先存储到本地目录，这里需做一下封装，后面重构时替换到S3/OSS要最小化代码改动
[Question] Q2: DLC 是否需要版本历史（同一个 dlcKey 可以有多个版本，支持回滚），还是只保留最新版本？
[Answer]
只保留最新
[Question] Q3: "已有玩家购买的 DLC 不允许删除"这个规则是否需要？还是允许强制删除？
[Answer]
保留

[Question] Q4: DLC 下载是否需要鉴权（只有购买过的玩家才能下载），还是管理端直接提供下载链接？
[Answer]
先简化，后续再加鉴权

[Question] Q5: 是否需要 DLC 的统计面板（下载趋势、收入统计）？
[Answer]
是
## 非功能性需求（待确认）

- 文件大小限制：单个 PCK 文件最大允许多少？（建议 500MB） 同意
- 并发上传：是否需要支持断点续传或分片上传？ 暂不支持，先简化
