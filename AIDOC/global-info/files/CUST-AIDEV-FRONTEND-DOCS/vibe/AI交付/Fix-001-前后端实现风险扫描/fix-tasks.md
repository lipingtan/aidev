# Fix 任务拆解 — Fix-001 第0批次（业务逻辑）

## 1. 批次目标

第0批次先完成业务逻辑正确性修复，再进入安全高危与工程化批次。

---

## 2. 任务清单

### A. 支付链路修复（优先级 P0）

- [x] A1. 前端移除直接触发 callback 的支付成功路径（`F-05`）
- [x] A2. 后端回调入口增加受控校验与幂等保护（`B-17` 相关）
- [x] A3. 支付详情增加 owner 归属校验（`B-18`）
- [x] A4. 退款增加可退额度与状态语义校验（`B-19`）
- [ ] A5. 补支付链路回归用例（成功/失败/并发/重复回调）

### B. 前端状态一致性修复（优先级 P1）

- [ ] B1. 401 统一清态（token/store/guard 缓存）并补回归（`F-01`）
- [ ] B2. 初始化守卫策略改为 fail-close 或维护态（`F-08`）
- [ ] B3. H5 增加认证态门禁与拦截提示（`F-09`）
- [ ] B4. 统一响应解包契约并修复误读页面（`F-10`）
- [ ] B5. 首页硬编码改真实数据或空态（`F-12`）

### C. 公告联动修复（优先级 P1，引用外部主计划）

- [ ] C1. 与 notice-center Fix-001 对齐快照与重推策略（`B-10`）
- [ ] C2. 联动验证：支付/登录态改动后公告 H5 可见性不回归

---

## 3. 依赖与并行关系

- 串行依赖：
  - A1 -> A2（前端路径调整需对齐后端回调策略）
  - A2 -> A5（回调策略稳定后再补回归）
  - B4 -> B5（解包契约稳定后再改首页数据消费）

- 可并行组：
  - 组1：A3 与 A4 可并行
  - 组2：B1、B2、B3 可并行
  - 组3：C1 与 B 组可并行（由不同模块负责人推进）

---

## 4. Definition of Done

- 第0批次所有任务完成并通过回归测试清单。
- `bugfix.md` 三段式需求均有可验证结果。
- 未引入已知主流程回归（登录、支付、公告、首页）。

