# 镜头制作工作流

> 系列 bootstrap 完成后，进入逐集/逐镜头的制作阶段。
> 本文件定义从单集策划到最终视频提示词的完整流程。

---

## 一、适用时机

```
series-bootstrap-workflow 全部 Phase 通过
    ↓
进入制作阶段（本文件）
    ↓
按集 → 按章节 → 按镜头 逐层展开
```

---

## 二、制作层级与文件对应

```
单集（Episode）         → story_plan.md + story_design.md
├── 章节（Chapter）     → chapter_plan.md + chapter_design.md
│   └── 镜头（Shot）   → shot_plan.md + shot_design.md → 提示词
```

**每一层统一模式**：plan（草案+澄清）→ 确认 → design（定稿+执行依据）

---

## 二-A、规范触发时机速查

> 以下规范不是"知道就行"，而是在特定步骤**主动查阅并落地到产出物中**。

| 规范文件 | 触发步骤 | 落地位置 | 具体动作 |
|----------|----------|----------|----------|
| `pacing.md` | story_design | 正式章节表的"情绪强度/节奏"列 | 根据叙事功能分配时长和节奏类型 |
| `pacing.md` | chapter_design | 片段表的时长列 | 每段时长符合叙事功能的建议范围 |
| `cinematography.md` | shot_plan | 景别/运镜字段 | 根据场景类型查决策表选镜头组合 |
| `character-consistency.md` | shot_design | 角色视觉字段 | 从 library/characters/ 逐词复制，不改写 |
| `character-consistency.md` | shot_design | 元素绑定字段 | 确保同一角色所有镜头绑定同一 ID |
| `style-keywords.md` | shot_design | Master Prompt 引用字段 | 从 style-bible 提取风格词（style-bible 本身引用此库） |
| `negative-prompts.md` | shot_design → 提示词组装 | 负面提示词字段 | 基础集(6个) + 场景追加(2-3个) |
| `tech.md` | shot_design | 各镜头设计表 | 确认镜头数/时长不超引擎限制 |
| `tech.md` | 提示词组装 | output/ 最终格式 | 按引擎格式组装（KLING 自包含 / WAN 时间戳） |
| `iteration-protocol.md` | 视频生成后 | iterations/ | 效果不满意时触发归因和迭代 |

### 各步骤必读清单

**story_plan/story_design**：
- `pacing.md` → 确定每章的节奏类型和时长分配

**chapter_plan/chapter_design**：
- `pacing.md` → 每段时长符合叙事功能
- `cinematography.md` → 跨段的景别交替规则

**shot_plan**：
- `cinematography.md` → 场景类型→镜头组合决策表
- `pacing.md` → 单镜头时长分配指南

**shot_design**：
- `character-consistency.md` → 角色描述逐词复制 + 元素绑定
- `style-keywords.md`（通过 style-bible）→ 风格锚定词
- `negative-prompts.md` → 组装负面提示词
- `tech.md` → 引擎限制（镜头数/时长/对话字数）

**提示词组装**（shot_design → output/）：
- `prompt-engineering.md` → 组装流程和格式
- `tech.md` → 最终格式规范

---

## 三、完整流程总览

```
story_plan → 澄清确认 → story_design（集级定稿）
    ↓
chapter_plan → 澄清确认 → chapter_design（章级定稿）
    ↓
shot_plan → 确认 → shot_design → 确认 → 生成提示词
```

每一层都遵循 **plan → 澄清 → 确认 → design** 的门控机制。

---

## 四、第一层：单集策划（story_plan → story_design）

### 4.1 输入依赖

- `production/library/pacing-map-{集名}.md`（节奏图）
- `production/library/series-bible.md`（系列规则）
- `foundation/scripts/故事细化.md`（对应集的故事节点）
- `foundation/scripts/大纲.md`（整体结构参考）

### 4.2 产出

```
AIDOC/series/{系列名}/production/{NNN}-{单集名}/
├── story_plan.md        # 集级策划（草案+澄清问题）
└── story_design.md      # 集级定稿（确认后的正式版）
```

### 4.3 story_plan 模板

```markdown
# 第{N}集：{集名} | Story Plan

## 已有素材

| 素材 | 路径 | 状态 |
|------|------|------|
| 大纲 | {路径} | ✅/❌ |
| 故事细化 | {路径} | ✅/❌ |
| 角色设定 | {涉及角色列表} | ✅/❌ |
| 视觉风格 | {路径} | ✅/❌ |
| 角色视觉 | {路径} | ✅/❌ |
| 节奏图 | {路径} | ✅/❌ |

## 本集概述

{一段话说明：核心内容、时间跨度、叙事目标、在全系列中的位置}

## 章节规划

| 章节 | 目录名 | 核心内容 | 预估时长 | 情绪走向 |
|------|--------|----------|----------|----------|
| 第1章 | ch01-{名称} | {概述} | X~Y分钟 | {情绪关键词} |
| 第2章 | ch02-{名称} | {概述} | X~Y分钟 | {情绪关键词} |
| ... | | | | |

**预估总时长**：{N}分钟

## 本集情绪弧线

```
情绪强度
  ↑
  │    {用文字描述各章的情绪起伏}
  └──────────────────────→ 时间
  ch01    ch02    ch03    ch04    ch05
```

## 澄清问题

### [Question] 1. 画面比例
16:9（横屏电影感）/ 9:16（竖屏短剧）/ 1:1

[Answer]

---

### [Question] 2. 拆分方式
A. 先做 chapter_plan，确认后再逐段做 shot_plan
B. 直接从第1章第1段开始逐段推进

[Answer]

---

### [Question] 3. 开场方式
A. 动作开场（直接进入事件）
B. 环境开场（先建立氛围再引入角色）
C. 闪前开场（先展示未来高潮片段）
D. 其他

[Answer]

---

### [Question] 4. 元素绑定状态
A. 已在 KLING 平台创建角色元素，只需填写注册表
B. 需要先生成参考图再创建元素
C. 暂不使用元素绑定

[Answer]

---

### [Question] 5. 对话量偏好
A. 尽量画面叙事，减少对话（更电影感）
B. 保留关键对话，用引擎原生对话功能
C. 混合——内心独白用旁白，角色互动用对话

[Answer]

---

## 状态
- [ ] Plan 已创建
- [ ] 用户已确认
- [ ] Story Design 已生成
```

### 4.4 story_design 模板（确认后的定稿）

> story_plan 中的澄清问题全部回答、用户确认后，生成 story_design 作为**集级正式执行版本**。

```markdown
# 第{N}集：{集名} | Story Design（正式版）

> 本文件为 story_plan.md 澄清确认后的正式产出。
> 后续所有 chapter_plan 以本文件为执行依据。

## 基本信息

| 属性 | 值 |
|------|-----|
| 集名 | {NNN}-{单集名} |
| 总时长 | {精确分钟数} |
| 总章节数 | {N} |
| 画面比例 | 16:9 / 9:16 |
| 生成引擎 | KLING AI / WAN 2.5 |
| 对话语言 | 英文 / 中文 |
| 对话策略 | {画面叙事为主 / 保留关键对话 / 混合} |
| 元素绑定 | {已有 / 需生成 / 不使用} |

## 确认的决策

| 决策项 | 结果 |
|--------|------|
| 画面比例 | {答案} |
| 开场方式 | {答案} |
| 对话量 | {答案} |
| 元素绑定 | {答案} |
| {其他澄清项} | {答案} |

## 正式章节表

| 章节 | 目录名 | 核心内容 | 时长 | 预估片段数 | 情绪强度 | 节奏 |
|------|--------|----------|------|-----------|----------|------|
| 第1章 | ch01-{名称} | {概述} | {X}分钟 | {N}段 | ★★★☆☆ | {快/中/慢} |
| 第2章 | ch02-{名称} | {概述} | {X}分钟 | {N}段 | ★★★★☆ | {快/中/慢} |
| ... | | | | | | |

**总时长**：{精确}分钟 | **总片段数**：约{N}段

## 全集情绪弧线

| 章节 | 情绪起点 | 情绪终点 | 关键转折 | 节奏类型 |
|------|----------|----------|----------|----------|
| ch01 | {起点} | {终点} | {转折事件} | {铺垫/冲突/高潮/余韵} |
| ch02 | {起点} | {终点} | {转折事件} | {类型} |
| ... | | | | |

## 跨章衔接设计

| 衔接 | 方式 | 说明 |
|------|------|------|
| ch01→ch02 | {硬切/渐变/动作衔接/时间跳跃} | {说明} |
| ch02→ch03 | {方式} | {说明} |
| ... | | |

## 全集元素绑定总览

| 角色/场景 | 出现章节 | 服装变体 | 绑定 ID |
|-----------|----------|----------|---------|
| {角色A} | ch01, ch02, ch05 | W1(日常), W3(战斗) | {ID} |
| {角色B} | ch01, ch03 | W1 | {ID} |
| {场景A} | ch01, ch05 | — | {ID} |
| ... | | | |

## 全集对话/音频规划

| 章节 | 有对话的片段 | 对话角色 | 语气/情绪 |
|------|-------------|----------|-----------|
| ch01 | ep-006, ep-007 | 艾薇儿 | 天真、担心 |
| ch01 | ep-012 | 维克多 | 阴冷、低语 |
| ... | | | |

## 状态
- [x] Story Design 已确认
- [ ] 开始逐章 chapter_plan
```

### 4.5 story_plan 与 story_design 的区别

| 维度 | story_plan | story_design |
|------|-----------|--------------|
| 性质 | 草案（含澄清问题） | 定稿（所有决策已锁定） |
| 章节表 | 预估时长和内容 | 精确时长、片段数、情绪强度 |
| 跨章衔接 | 不含 | 包含章与章的转场设计 |
| 元素绑定 | 只确认状态（有/无） | 包含全集绑定 ID 总览 |
| 对话规划 | 只确认偏好 | 包含具体哪些片段有对话 |
| 情绪弧线 | 粗略描述 | 精确到每章的起止和转折 |
| 门控 | 需要用户确认 | 已确认，可直接执行 |

### 4.6 门控

- story_plan 中的澄清问题全部回答 → 生成 story_design
- 用户确认 story_design → 逐章节进入 chapter_plan

---

## 五、第二层：章节策划与定稿（chapter_plan → chapter_design）

### 5.1 输入依赖

- 已确认的 `story_plan.md`（章节划分和时长分配）
- `foundation/scripts/故事细化.md`（对应章节的详细节点）
- `production/library/` 中的角色/场景/风格资产
- `production/library/pacing-map-{集名}.md`（节奏参考）

### 5.2 产出

```
AIDOC/series/{系列名}/production/{NNN}-{单集名}/{章节名}/
├── chapter_plan.md      # 章节策划（含澄清问题）
└── chapter_design.md    # 章节定稿（确认后的正式版）
```

### 5.3 chapter_plan 模板

```markdown
# 第{N}章：{章名} | Chapter Plan

## 参考文件
> 生成本章内容前，必须读取以下文件：
- {列出所有依赖文件}

## 章节信息

| 属性 | 值 |
|------|-----|
| 章节 | {目录名} |
| 所属 | {集名} |
| 预估时长 | {X~Y分钟} |
| 核心主题 | {一句话} |
| 涉及角色 | {角色列表} |
| 涉及场景 | {场景列表} |
| 情绪弧线 | {情绪变化关键词} |

## 大纲节点
来自故事细化的对应节点（编号列出）：
1. {节点描述}
2. ...

## 片段拆分方案

总时长约{X}秒。按 Part 分组：

### Part A：{分组名}（{N}段，约{X}秒）

| 片段ID | 镜头数 | 时长 | 核心画面 | 角色 | 场景 |
|--------|--------|------|----------|------|------|
| ep-{NNN} | {N}镜头 | {X}s | {画面描述} | {角色(服装)} | {场景} |

### Part B：...

## 澄清问题

### [Question] 1. {问题}
{选项/说明}

[Answer]
{用户回答后填入}
---

## 状态
- [ ] Chapter Plan 已创建
- [ ] 用户已确认
- [ ] 开始逐段 shot_plan
```

### 5.4 chapter_design 模板（确认后的定稿）

> chapter_plan 中的澄清问题全部回答、用户确认后，生成 chapter_design 作为**正式执行版本**。

```markdown
# 第{N}章：{章名} | Chapter Design（正式版）

> 本文件为 chapter_plan.md 澄清确认后的正式产出。所有片段按此文档执行。

## 参考文件
> 执行本章任何片段前，必须读取以下文件：
- {列出所有依赖文件}

## 基本信息

| 属性 | 值 |
|------|-----|
| 章节 | {目录名} |
| 总时长 | {精确秒数} |
| 总片段数 | {N} |
| 总镜头数 | {N} |
| 生成模式 | KLING 多镜头 / WAN 时间戳 |
| 画面比例 | 16:9 / 9:16 |
| 对话语言 | 英文 / 中文 |

## 确认的决策

| 决策项 | 结果 |
|--------|------|
| {从澄清问题中提取} | {确认的答案} |

## 正式片段表

### Part A：{分组名}（{N}段，{X}秒）

| 片段 | 镜头 | 时长 | 画面 | 角色 | 场景 | 元素绑定 |
|------|------|------|------|------|------|----------|
| ep-{NNN} | {N} | {X}s | {画面描述} | {角色(服装)} | {场景} | {绑定ID列表} |

### Part B：...

## 转场表

| 衔接 | 方式 | 说明 |
|------|------|------|
| ep-{N}→ep-{N+1} | {硬切/动作衔接/渐变/...} | {说明} |

## 状态
- [x] Chapter Design 已确认
- [ ] 开始逐段 shot_plan → shot_design → 提示词
```

### 5.5 chapter_plan 与 chapter_design 的区别

| 维度 | chapter_plan | chapter_design |
|------|-------------|----------------|
| 性质 | 草案（含澄清问题） | 定稿（所有决策已锁定） |
| 片段表 | 初步方案（可能调整） | 正式版（按此执行） |
| 元素绑定 | 不含 | 包含每段的元素绑定 ID |
| 转场表 | 不含 | 包含完整转场设计 |
| 时长 | 预估范围 | 精确秒数 |
| 门控 | 需要用户确认 | 已确认，可直接执行 |

### 5.6 门控

- chapter_plan 中的澄清问题全部回答 → 生成 chapter_design
- 用户确认 chapter_design → 逐镜头进入 shot_plan

---

## 六、第三层：镜头策划与设计（shot_plan → shot_design）

### 6.1 输入依赖

- 已确认的 `chapter_design.md`（正式片段表）
- `production/library/characters/{角色}.md`（角色视觉关键词）
- `production/library/scenes/{场景}.md`（场景视觉描述）
- `production/library/style-bible.md`（风格锚定）
- `production/library/elements-registry.md`（元素绑定 ID）

### 6.2 产出

```
{章节名}/shots/ep-{NNN}/
├── shot_plan.md       # 镜头策划（中文，策划层）
├── shot_design.md     # 镜头设计（中英混合，设计层）
└── iterations/        # 迭代记录（如有）
    └── v{N}.md
```

### 6.3 shot_plan 模板

```markdown
## Shot Plan: ep-{NNN}

### 叙事目的
本镜头要传达什么信息/情绪？

### 画面概述（中文）
一句话描述画面内容。

### 时长
{X} 秒

### 镜头数
{N} 镜头（KLING 多镜头模式）

### 各镜头分解
| 镜头 | 时长 | 景别 | 运镜 | 画面内容 |
|------|------|------|------|----------|
| ① | {X}s | {景别} | {运镜} | {描述} |
| ② | {X}s | {景别} | {运镜} | {描述} |
| ... | | | | |

### 角色
- 角色A（服装）：动作描述
- 角色B（服装）：动作描述（如有）

### 环境/光线
场景名 + 光线状态

### 情绪基调
{关键词}

### 与前后镜头的衔接
- 承接上一镜头：{动作/视线/情绪}
- 过渡到下一镜头：{动作/视线/情绪}

### 对话/音效
- 台词："{内容}"（如有）
- 环境音：{描述}
```

**门控**：用户确认 shot_plan 后进入 shot_design。

### 6.4 shot_design 模板

```markdown
## Shot Design: ep-{NNN}

### 引擎
KLING AI / WAN 2.5

### Master Prompt 引用
{引用 style-bible 中的风格锚定词}

### 角色视觉（从 library/characters/ 提取）
- 角色A：{英文视觉关键词}
- 角色B：{英文视觉关键词}

### 场景视觉（从 library/scenes/ 提取）
{英文场景描述}

### 各镜头设计

| 镜头 | 时长 | 景别+运镜 | 动作描述（英文） | 光线 |
|------|------|-----------|-----------------|------|
| ① | {X}s | {shot type, camera move} | {action} | {lighting} |
| ② | {X}s | {shot type, camera move} | {action} | {lighting} |

### 情绪氛围（英文）
{氛围关键词}

### 元素绑定
- 角色A → elements-registry 中的 ID：{ID}
- 场景 → elements-registry 中的 ID：{ID}

### 负面提示词
{全局负面 + 场景特定负面}

### 预期时长
{X} 秒

### 对话音频
- 台词："{英文}"
- 语气：{tone}
```

**门控**：用户确认 shot_design 后生成最终提示词。

### 6.5 生成提示词

**输入**：已确认的 shot_design
**产出**：`output/{系列名}/kling/{NNN}-{单集}/{章节}/ep-{NNN}.md`

按 `prompt-engineering.md` 的组装流程生成最终提示词。

**规则**：
- output/ 中的文件是纯提示词，无注释无说明
- 可直接复制粘贴到生成工具使用
- 同步创建 `output_video/` 对应目录结构

---

## 七、各层级的确认粒度

| 层级 | 确认方式 | 可跳过确认的情况 |
|------|----------|-----------------|
| story_plan | 必须逐项确认 | 无（首次必须） |
| chapter_plan | 必须确认（含澄清） | 无 |
| chapter_design | 必须确认 | 无（这是执行依据） |
| shot_plan | 逐个确认 | 氛围/建立镜头可批量确认 |
| shot_design | 逐个确认 | 用户明确说"按 plan 直接出 design"时可跳过 |
| 提示词生成 | 无需确认 | design 确认即可自动生成 |

---

## 八、迭代优化流程

### 8.1 触发条件

视频生成后效果不满意。

### 8.2 流程

```
1. 记录问题（iterations/ 目录）
2. 归因分析（参见 iteration-protocol.md）：
   - 提示词问题 → 修改 shot_design → 重新生成提示词
   - 引擎限制 → 调整策略（拆分镜头/换景别/简化动作）
   - 风格偏移 → 检查 style-bible 锚定词是否遗漏
3. 修改后更新 output/ 中的提示词（旧版本移入 iterations/）
4. 重新生成视频
```

### 8.3 详细迭代规范

参见 `iteration-protocol.md`（归因决策树、迭代记录格式、策略升级规则）。

---

## 九、批量制作效率规则

| 场景 | 策略 |
|------|------|
| 同一场景多个镜头 | 共享 Master Prompt，只改 Shot Prompt |
| 同一角色多个镜头 | 确保元素绑定 ID 一致 |
| 连续对话镜头 | 保持 180 度法则，交替正反打 |
| 氛围/建立镜头 | 可批量设计，无需逐个确认 |
| 同一 Part 内的镜头 | 可一次性生成所有 shot_plan，批量确认 |

---

## 十、进度追踪

更新 `production/production-tracker.md`：

```markdown
| 集 | 章节 | 镜头 | plan | design | prompt | video | 状态 |
|----|------|------|:----:|:------:|:------:|:-----:|------|
| 001 | ch01 | ep-001 | ✅ | ✅ | ✅ | ✅ | 完成 |
| 001 | ch01 | ep-002 | ✅ | ✅ | ✅ | ⚠️v2 | 迭代中 |
| 001 | ch01 | ep-003 | ✅ | 待确认 | - | - | 设计中 |
```

---

## 十一、完整目录结构示例

```
AIDOC/series/{系列名}/production/{NNN}-{单集名}/
├── story_plan.md                           # 集级策划（草案+澄清）
├── story_design.md                         # 集级定稿（正式执行版）
├── synopsis.md                             # 本集概要（可选）
├── assembly.md                             # 组装顺序（后期用）
│
├── {ch01-章节名}/
│   ├── chapter_plan.md                     # 章级策划（含澄清）
│   ├── chapter_design.md                   # 章级定稿（正式执行版）
│   └── shots/
│       ├── ep-000/
│       │   ├── shot_plan.md
│       │   ├── shot_design.md
│       │   └── iterations/
│       │       └── v2.md
│       ├── ep-001/
│       │   ├── shot_plan.md
│       │   └── shot_design.md
│       └── ...
│
├── {ch02-章节名}/
│   ├── chapter_plan.md
│   ├── chapter_design.md
│   └── shots/
│       └── ...
│
└── README.md
```
