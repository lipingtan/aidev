# Go 软件开发工作流程

## 强制执行声明（对所有 AI 实体生效）

> **本规范对主代理、子代理、任何被委托的 AI 实体均具有约束力。**
> 子代理在执行任务时，必须遵循本文档定义的阶段门控和用户确认要求，不得以"已由上级代理确认"为由跳过确认步骤。

---

## CR 复杂度判定与流程要求

### 复杂 CR 定义（命中任一即为复杂）

1. 业务逻辑涉及多个条件分支或状态转换
2. 跨 ≥2 个模块（不同 app/ 子目录）的联动修改
3. 涉及数据库 DDL 变更 + 业务逻辑修改
4. 涉及权限/认证/多租户等安全相关变更
5. 新增完整 CRUD 接口（≥4 个端点）
6. 涉及第三方系统集成或异步流程

### 流程要求

| CR 类型 | 强制流程 |
|---------|---------|
| **复杂 CR** | 需求计划 → 用户确认 → 需求 → 设计计划 → 用户确认 → 设计 → 任务 → 执行 |
| **简单 CR** | 需求 → 设计 → 任务 → 执行（可合并计划到正式文档） |

**判定规则：** 不确定时按复杂 CR 处理。

---

## 阶段门控规则（强制）

**核心原则：每个阶段的产出物必须经用户明确确认后，才能进入下一阶段。**

| 门控点 | 确认内容 | 未确认时禁止 |
|--------|----------|-------------|
| 需求计划完成 | AI 对需求的理解是否正确、澄清问题的答复 | 编写 requirements.md |
| 需求文档完成 | 用户故事/功能需求/验收标准是否完整准确 | 编写 design_plan.md |
| 设计计划完成 | 技术方案方向/设计问题的答复 | 编写 design.md |
| 设计文档完成 | API/数据库/核心逻辑/不变行为清单是否正确 | 编写 tasks.md |
| 任务列表完成 | 任务拆分/依赖顺序/验收标准是否合理 | 开始执行 |

**违反门控 = 流程错误，必须回退到未确认的阶段重新执行。**

---

## 角色定义

| 阶段 | 角色 |
|------|------|
| Requirement | 产品经理，擅长需求分析、用户故事、验收标准定义 |
| Design | Go 后端架构师，精通 Gin/GORM/go-admin 架构设计、API 设计、数据库设计 |
| Task | 技术项目经理，擅长任务拆解、依赖分析、并行规划 |
| 执行 | Go 高级开发工程师，精通 Go 1.21+、Gin、GORM、多租户架构 |
| Bugfix | 软件质量工程师，擅长缺陷分析、根因定位 |

---

## 1. Requirement 阶段

### 1.1 需求计划（requirements_plan.md）

**目的：** 确保 AI 正确理解用户意图，在编写正式需求前达成共识。

**流程：**
1. 创建 `requirements_plan.md`
2. **必须包含以下内容：**
   - **需求理解陈述**：用 AI 自己的话复述对需求的理解（目标、范围、预期效果）
   - **假设列表**：AI 做出的所有假设，标记 `[假设-N]`
   - **澄清问题**：用 `[Question-N]` 标签提出，创建空的 `[Answer-N]` 标签
   - **非功能性需求建议**：性能、安全、多租户隔离等
   - **影响范围预判**：预估涉及的模块和文件
3. 完成后请求用户审查
4. **用户必须：**
   - 确认或修正 AI 的理解陈述
   - 回答所有 `[Question]`
   - 确认或否决 `[假设]`
5. **全部确认后**，才进入 1.2

### 1.2 生成需求文档（requirements.md）

**前置条件：** requirements_plan.md 已获用户确认。

**流程：**
1. 基于确认后的计划，输出中文 `requirements.md`
2. 用户审查需求文档
3. 用户确认后，进入 Design 阶段

### 1.2 需求文档格式

```markdown
# 需求：{功能名称}

## 背景
{业务背景和目标}

## 用户故事
- 作为 {角色}，我希望 {功能}，以便 {价值}

## 功能需求
### FR-1: {需求名}
**描述：** ...
**验收标准：**
- WHEN {条件} THEN 系统 SHALL {行为}

## 非功能需求
- 性能：接口响应时间 < 200ms（P99）
- 安全：所有接口需 JWT 认证
- 多租户：数据按 tenant_id 隔离
```

---

## 2. Design 阶段

### 2.1 设计计划（design_plan.md）

**前置条件：** requirements.md 已获用户确认。

**流程：**
1. 创建 `design_plan.md`
2. **必须包含以下内容：**
   - **设计方向陈述**：基于需求，AI 阐述拟采用的技术方案方向
   - **技术选型理由**：为何选择特定方案（如有多种可行方案需列出比较）
   - **澄清问题**：用 `[Question-N]` 标签提出设计相关疑问
   - **风险点**：标识可能的技术风险或兼容性问题
3. 完成后请求用户审查
4. **用户必须确认设计方向和回答澄清问题后**，才进入 2.2

### 2.2 生成设计文档（design.md）

**前置条件：** design_plan.md 已获用户确认。

**流程：**
1. 基于确认后的设计计划，输出 `design.md`
2. 用户审查设计文档
3. 用户确认后，进入 Task 阶段

### 2.2 设计文档必须包含

```markdown
# 设计：{功能名称}

## 技术方案
### API 设计
| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| GET | /api/v1/game | 游戏列表 | JWT + 租户 |

### 数据库设计
```sql
-- 新增/修改的表结构
ALTER TABLE game ADD COLUMN xxx ...;
```

### 核心逻辑
{流程图或伪代码}

## 不变行为清单（回归防护）
| 编号 | 不变行为 | 验证方式 |
|------|----------|----------|
| RG-1 | 现有登录流程不受影响 | /login 接口正常返回 token |
| RG-2 | 租户隔离不被破坏 | 不同租户数据不互通 |

## 正确性属性
- 所有写操作必须记录 create_by/update_by
- 业务表查询必须带 tenant_id 过滤
```

---

## 3. Task 阶段

### 3.1 任务复杂度判定

按以下顺序匹配，命中即停：
1. 涉及跨模块调用或新增接口 → **高**
2. 涉及数据库 DDL 变更 → **高**
3. 涉及 ≥3 个文件的联动修改 → **中**
4. 有条件分支或业务规则判断 → **中**
5. 其余 → **低**

### 3.2 任务三要素格式

> 详细定义见 `go-task-representation.md`，以下为各复杂度的格式示例。

**高复杂度任务（示例）：**
```markdown
### Task N: {任务名}

**复杂度**: 高

**Scope（边界）:**
- 涉及文件: app/game/apis/game.go, app/game/service/game.go, app/game/service/dto/game.go
- 涉及模块: game
- 不触碰: common/middleware/tenant.go、其他域的 service

**Constraints（约束）:**
- 必须使用 middleware.GetTenantId(c) 获取租户 ID，不从请求参数读取
- 查询必须加 WHERE tenant_id = ? 过滤（tenant_id=0 时不过滤）
- 错误必须用 fmt.Errorf 包装并返回，不得忽略

**Acceptance（验证标准）:**
- AC: 接口返回正确的分页数据
- AC: 不同租户数据互相隔离
- AC: go build ./... 零错误
- AC: 【回归】现有接口不受影响（RG-1）
```

**中复杂度任务（示例）：**
```markdown
### Task N: {任务名}

**复杂度**: 中

**Scope（边界）:**
- 涉及文件: app/game/models/game.go, app/game/service/dto/game.go
- 不触碰: 其他 model 文件

**Acceptance（验证标准）:**
- AC: Model 字段定义正确，包含 GORM tag
- AC: DTO 与 Model 字段对应
- AC: go build ./... 零错误
```

**低复杂度任务（示例）：**
```markdown
### Task N: {任务名}

**复杂度**: 低

**Acceptance:**
- AC: 修改内容符合需求描述
- AC: go build ./... 零错误
```

---

## 4. Task 执行阶段

### 4.1 执行前（必须）

1. 读取任务的三要素（Scope/Constraints/Acceptance）
2. 确认涉及文件范围和禁区
3. 确认技术和行为约束
4. 三要素不完整时先澄清再执行

### 4.2 执行中（必须）

1. 严格在 Scope 范围内操作
2. 遵循 Constraints 中的所有约束
3. 需要修改 Scope 之外的文件时，先报告用户确认

### 4.3 执行后（必须）

1. 运行 `go build ./...` 确认零错误
2. 运行 `go vet ./...` 确认无警告
3. 逐条验证 Acceptance 中的每个标准
4. 全部通过才标记任务为已完成
5. 回归验证项无法自动验证时，标注"需人工确认"

---

## 5. Bugfix 流程

### 5.1 文件存放位置

```
AIDOC/project_doc/{project-name}/Fix-{N}-{bug简述}/
├── bugfix.md       # bug 分析文档
├── design.md       # 修复设计
└── tasks.md        # 修复任务
```

### 5.2 Bugfix 文档格式

```markdown
# Bugfix: {bug 简述}

## Bug 分析
**当前行为（缺陷）：**
WHEN {触发条件} THEN 系统 {错误表现}

**期望行为（正确）：**
WHEN {触发条件} THEN 系统 SHALL {正确行为}

**不变行为（回归防护）：**
WHEN {相关场景} THEN 系统 SHALL CONTINUE TO {保持行为}

## 根因分析
{定位到具体文件和代码行}

## 影响范围
{涉及的模块、接口、数据}
```

### 5.3 Go 常见 Bug 排查清单

**第一优先级：基础设施层**
1. **JSON 反序列化问题**
   - `time.Time` 字段格式不匹配
   - 指针类型 vs 值类型（`*int` vs `int`）
   - 枚举值不在预期范围内

2. **中间件链问题**
   - JWT 验证失败（token 格式、过期）
   - 租户 ID 未正确注入（`WithTenantId` 中间件顺序）
   - 数据库连接未初始化（`WithContextDb` 中间件）

**第二优先级：业务逻辑层**
3. **nil pointer dereference**
   - 数据库查询结果未判空
   - 接口类型断言失败
   - 未初始化的 map/slice

4. **GORM 查询问题**
   - 忘记加 `tenant_id` 过滤
   - 软删除字段未正确配置
   - 事务未正确提交/回滚

---

## 文件结构

每个需求在对应的 project_doc 目录下产出：

```
AIDOC/project_doc/{project-name}/{feature-name}/
├── requirements.md     # 需求文档
├── design.md           # 设计文档
└── tasks.md            # 任务列表
```

Bugfix 文档：
```
AIDOC/project_doc/{project-name}/Fix-{N}-{bug简述}/
├── bugfix.md
├── design.md
└── tasks.md
```

工程代码放在：
```
projects/{解决方案名}/{项目名}/    # 解决方案下的工程目录
```

---

## 相关规范索引

| 规范 | 关系 |
|------|------|
| `go-project-bootstrap.md` | 新项目从零启动的 Phase 0 流程 |
| `go-conventions.md` | 编码规范（命名/格式/注释/错误处理） |
| `go-project-structure.md` | 目录结构和分层架构 |
| `go-task-representation.md` | 任务三要素的详细定义 |
| `go-regression-guard.md` | 回归防护规范 |
| `go-api-design.md` | RESTful API 设计规范 |
| `go-testing.md` | 测试编写规范 |
| `go-deployment.md` | 部署与发布流程 |
| `go-git-workflow.md` | Git 分支和提交规范 |
| `go-security.md` | 安全编码规范 |
| `go-code-review.md` | 代码审查规范 |
| `go-debugging.md` | 排查方法论 |

---

## 附录 A：需求计划模板

```markdown
# 需求计划：{功能名称}

## 需求理解

{用 AI 自己的话复述对需求的理解，包括：}
- 目标：{这个需求要解决什么问题}
- 范围：{涉及哪些模块/功能}
- 预期效果：{完成后用户能做什么}

## 假设列表

- [假设-1] {AI 做出的假设，例如：假设用户角色权限沿用现有 RBAC 模型}
- [假设-2] ...

## 澄清问题

- [Question-1] {问题}
  [Answer-1]
- [Question-2] {问题}
  [Answer-2]

## 非功能需求建议

- 性能：...
- 安全：...
- 多租户：...

## 影响范围预判

- 涉及模块：...
- 涉及文件（预估）：...
- 可能的副作用：...
```

## 附录 B：设计计划模板

```markdown
# 设计计划：{功能名称}

## 设计方向

{基于确认的需求，阐述拟采用的技术方案方向}

## 技术选型

| 选项 | 优点 | 缺点 | 推荐 |
|------|------|------|------|
| 方案 A | ... | ... | ✓/✗ |
| 方案 B | ... | ... | ✓/✗ |

## 澄清问题

- [Question-1] {设计相关疑问}
  [Answer-1]

## 风险点

- [Risk-1] {技术风险或兼容性问题}
- [Risk-2] ...
```
