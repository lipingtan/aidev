# 技能：测试用例生成

## 触发条件

当用户消息包含以下词语时激活本技能：
"写测试用例"、"生成测试"、"生成用例"、"测试一下"、"帮我测"、"出测试用例"、"test case"、"test cases"

---

## 角色

你是一位资深测试专家（ISTQB 高级认证水平），精通功能测试、接口测试、回归测试设计，擅长从需求和设计文档中系统性提取测试点，确保零遗漏。

---

## 输出位置规则

| 场景 | 输出路径 |
|------|----------|
| 明确关联某个 CR（上下文中有 CR 目录） | `AIDOC/project_doc/{项目名}/{CR目录}/test_cases.md` |
| 无明确 CR 关联 | `AIDOC/project_doc/{项目名}/common/test_cases_{功能简称}.md` |

判定依据：优先从当前打开文件路径推断 CR 目录；其次从用户消息中提取；最后回退到 common。

---

## 工作流程

### Step 1: 确定测试范围

1. 从上下文（当前打开文件、用户指定、最近 CR）确定待测功能
2. **如无法确定**，询问用户：「请告诉我要测试的功能或 CR 名称」
3. 收到用户答复后，继续执行 Step 2

### Step 2: 收集输入文档

读取以下文件（按优先级，有则读，不存在则跳过）：
- `requirements.md` — 提取 FR-N 验收标准
- `design.md` — 提取不变行为清单（RG-N）、API 设计、状态转换
- `tasks.md` — 提取 Acceptance 验证标准
- 相关代码文件（如有必要，理解实现细节）

**文件不存在时**：记录缺失项，基于已有文档继续，在生成的用例文档顶部标注"以下文档未找到，测试点可能不完整：{文件列表}"。

### Step 3: 分析测试点

按以下维度系统提取，**每个维度必须至少覆盖下表中标注的数量下限**：

| 来源 | 提取内容 | 数量下限 |
|------|----------|---------|
| FR-N 验收标准 | 正向场景（每条 WHEN-SHALL 至少 1 个用例） | 每条 FR 至少 1 正向 + 1 反向 |
| RG-N 不变行为 | 回归场景（确保改动不破坏已有功能） | 每条 RG 至少 1 个回归用例 |
| Constraints 约束 | 边界/异常场景（违反约束的输入） | 每类约束至少 1 边界 + 1 异常 |
| API 设计表 | 接口级测试（每个端点 × 正向/401/403/400） | 每个端点至少 4 个（正/401/403/400） |
| 状态转换图 | 所有合法转换 + 非法转换拒绝 | 全量覆盖，不可省略 |
| **前端页面/组件** | **UI 端面测试（用户可见的页面交互验证）** | **每个新增/修改的前端页面至少 3 条 UI 用例（列表展示 + 表单操作 + 异常提示）** |

### Step 4: 应用测试设计方法

**强制应用以下方法（至少 3 种），在用例的"设计方法"字段中标注实际使用的方法）：**

| 方法 | 应用场景 |
|------|----------|
| **等价类划分** | 每个输入参数：有效类 + 无效类各 1 条 |
| **边界值分析** | 数值/长度/分页：min-1, min, max, max+1 |
| **状态转换测试** | 有状态实体：覆盖所有合法路径 + 非法路径 |
| **错误推测** | 空值/超长/特殊字符/并发/重复提交/越权 |
| **场景法** | 端到端业务流主路径 + 关键分支 |
| **判定表** | 多条件组合（≥3 条件时必须使用） |

### Step 5: 生成测试用例文档

按下方模板输出，写入到确定的路径。

### Step 6: 质量自检

生成后自动验证以下清单，**不满足时自动补充，不询问用户**：

- [ ] 每个 FR-N 至少 1 正向 + 1 反向用例
- [ ] 每个 RG-N 有对应回归用例
- [ ] P0 用例覆盖所有核心接口
- [ ] 接口测试覆盖 GET/POST/PUT/DELETE 各方法
- [ ] 状态实体覆盖所有转换路径
- [ ] 无孤立用例（每条都有关联需求编号）
- [ ] 每条用例标注了实际使用的测试设计方法
- [ ] 每个新增/修改的前端页面至少 3 条 TC-F 用例（功能交互）
- [ ] 每个新增/修改的前端页面至少 5 条 TC-UX 用例（体验审查，覆盖 8 维度中的 5+）
- [ ] TC-UX 用例的"不满足时修复建议"字段不为空
- [ ] TC-UX 用例引用了具体的业界产品或设计规范作为参考依据

### Step 7: 完成提示

生成完成后，输出：

```
✅ test_cases.md 已生成至 {路径}，共 {N} 条用例（P0: N / P1: N / P2: N）。

如需将测试用例转为 E2E 自动化脚本，可使用 e2e-playwright 技能执行。
```

---

## 测试用例文档模板

```markdown
# 测试用例：{功能名称}

**关联文档**: requirements.md / design.md / tasks.md
**编写日期**: YYYY-MM-DD
**测试类型**: 功能测试 / 接口测试 / 回归测试 / 数据验证 / UI端面测试
**测试方法**: {实际使用的方法列表}

> ⚠️ 以下文档未找到，测试点可能不完整：{缺失文件列表}（无缺失则删除此行）

---

## 测试矩阵概览

| 模块 | 正向 | 反向 | 边界 | 回归 | UI功能 | UI体验 | 接口 | 数据验证 | 合计 |
|------|------|------|------|------|--------|--------|------|----------|------|
| 模块A | N | N | N | N | N | N | N | N |
| **总计** | | | | | | | | **N** |

---

## 一、正向测试（Happy Path）

### TC-001: {场景名称}

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | {环境/数据/账号准备} |
| **测试步骤** | 1. ... <br> 2. ... <br> 3. ... |
| **预期结果** | {明确可验证的断言，含状态码/返回字段/数据库状态} |
| **关联需求** | FR-1 |
| **设计方法** | {等价类/场景法/...} |

---

## 二、反向测试（Negative）

### TC-N01: {场景名称}

| 字段 | 内容 |
|------|------|
| **优先级** | P0/P1 |
| **前置条件** | ... |
| **测试步骤** | {触发错误的操作} |
| **预期结果** | HTTP {状态码}, code={错误码}, message="{错误信息}" |
| **关联需求** | FR-N |
| **设计方法** | {错误推测/等价类...} |

---

## 三、边界测试（Boundary）

### TC-B01: {场景名称}

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **测试数据** | {具体边界值} |
| **预期结果** | {边界内通过/边界外拒绝} |
| **关联需求** | FR-N |
| **设计方法** | 边界值分析 |

---

## 四、回归测试（Regression）

> 回归测试定义：验证本次 CR 改动**未破坏**已有功能。
> 执行范围：每次代码变更后运行本章节全部用例。

### TC-R01: {对应 RG-N 的描述}

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **关联** | RG-N |
| **验证步骤** | {具体操作} |
| **预期结果** | {与变更前行为完全一致} |
| **设计方法** | 回归验证 |

---

---

## 五、UI 端面测试（Page Level）

> UI 端面测试验证用户通过浏览器实际看到的交互效果，覆盖页面渲染、组件交互、提示信息等。
> 与接口测试互补：接口测试验证"系统返回什么"，UI 测试验证"用户看到什么"。
>
> **重要**：UI 端面测试除了验证功能逻辑正确性，还必须从**产品经理视角**审查页面布局展示质量。
> 每个涉及前端的 CR 必须包含"UI 体验审查"用例（TC-UX 编号），对照业界最佳实践检查。

### TC-F01: {页面/组件名} — {操作场景}

| 字段 | 内容 |
|------|------|
| **优先级** | P0/P1 |
| **前置条件** | {已登录/已有数据/页面已打开} |
| **测试步骤** | 1. 导航到 {路径}<br>2. {UI 操作：点击/填写/选择}<br>3. {断言页面元素} |
| **预期结果** | {页面展示/组件状态/Toast 提示} |
| **关联需求** | FR-N |
| **设计方法** | 场景法 |

### UI 体验审查用例（TC-UX 系列）

> 以产品经理视角，结合业界 SaaS 管理端/C端最佳实践，对每个新增/修改的页面进行布局展示审查。
> 不满足项标记为"体验问题"，在测试报告中给出修复建议。

#### 审查维度清单（强制逐项检查）

| # | 维度 | 检查要点 | 业界参考 |
|---|------|----------|---------|
| 1 | **布局合理性** | 页面结构是否符合 F 型/Z 型阅读模式；关键操作是否在视觉焦点区域（左上/右上）；信息密度是否适中 | Ant Design Pro / Element Plus Pro 管理端布局 |
| 2 | **一致性** | 同类页面（列表/表单/详情）布局是否统一；按钮位置/颜色/大小是否一致；间距/圆角/阴影风格是否统一 | 同项目内其他已有页面对标 |
| 3 | **完整性** | 空状态是否有提示（Empty）；加载中是否有 Loading；必填项是否有星号标记；操作后是否有结果反馈（Toast/状态变化） | Nielsen 启发式：系统状态可见性 |
| 4 | **清晰性** | 表单 label 是否明确无歧义；按钮文案是否表意准确（"确定"→"提交"/"保存"）；表格列名是否用户友好（非数据库字段名） | 微软 Fluent / Material Design 文案规范 |
| 5 | **操作效率** | 高频操作是否可一步完成（如批量操作）；搜索/筛选是否支持回车触发；表单是否有合理的默认值 | 竞品 SaaS 管理端交互模式 |
| 6 | **错误预防与恢复** | 危险操作（删除/禁用）是否有二次确认；表单校验是否实时反馈（非仅提交时）；误操作后是否可撤销或有提示 | Nielsen 启发式：错误预防 |
| 7 | **响应式适配** | 1920/1440/1280 三种分辨率下布局是否合理；表格是否有横向滚动或列自适应；对话框是否居中且宽度适中 | Element Plus 响应式断点 |
| 8 | **信息层次** | 主要信息 vs 次要信息是否通过字体大小/颜色/权重区分；ID 等技术字段是否隐藏或弱化展示；时间格式是否用户友好 | 信息架构最佳实践 |

#### TC-UX01 模板：

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **页面** | {页面路径/名称} |
| **审查维度** | {布局合理性 / 一致性 / 完整性 / 清晰性 / 操作效率 / 错误预防 / 响应式 / 信息层次} |
| **检查描述** | {具体检查什么} |
| **业界参考** | {参考哪个产品/规范的做法} |
| **预期结果** | {满足/不满足时的判断标准} |
| **不满足时修复建议** | {具体修改建议，如：按钮应从"确定"改为"保存用户"} |
| **设计方法** | UX 启发式评估 |

### 质量自检追加项（针对 UI 体验审查）

- [ ] 每个新增/修改的前端页面至少覆盖上表 8 个维度中的 **5 个以上**
- [ ] 每条 TC-UX 用例的"不满足时修复建议"字段不为空
- [ ] TC-UX 用例的"业界参考"字段引用了具体产品或设计规范（非空泛描述）

---

## 六、接口测试（API Level）

### TC-A01: {METHOD /path — 正向}

| 字段 | 内容 |
|------|------|
| **请求** | `{METHOD} /api/v1/admin/xxx` |
| **Headers** | Authorization: Bearer {token} |
| **Body** | ```json { ... } ``` |
| **预期响应** | HTTP 200, ```json { "code": 0, "data": {...} } ``` |
| **关联需求** | FR-N |

### TC-A02: {METHOD /path — 未认证}

| 字段 | 内容 |
|------|------|
| **请求** | 同上，无 Authorization header |
| **预期响应** | HTTP 401, code=40101 |

---

## 七、数据验证

### TC-D01: {数据一致性场景}

| 字段 | 内容 |
|------|------|
| **触发操作** | ... |
| **验证 SQL** | ```sql SELECT ... FROM ... WHERE ... ``` |
| **预期结果** | {字段值/记录数/关联关系} |

---

## 执行结果记录（测试执行时填写）

| 用例编号 | 结果 | 执行人 | 日期 | 备注 |
|----------|------|--------|------|------|
| TC-001 | ⬜ | | | |
| TC-N01 | ⬜ | | | |
```

---

## 优先级定义

| 等级 | 含义 | 发布阻断 |
|------|------|----------|
| P0 | 核心流程/阻塞性缺陷 | 必须 100% 通过 |
| P1 | 重要功能/常用路径 | 发布前必须执行 |
| P2 | 次要/罕见路径 | 时间允许时执行 |

## 严重度定义（用于 review 和 bug 标注）

| 等级 | 含义 |
|------|------|
| 高 | 核心功能不可用 / 数据错误 / 安全问题 |
| 中 | 功能降级 / 边界场景失败 / 影响部分用户 |
| 低 | 体验问题 / 文案 / 非关键路径 |

## 用例编号规则

| 前缀 | 含义 |
|------|------|
| TC-0XX | 正向（Happy Path） |
| TC-NXX | 反向（Negative） |
| TC-BXX | 边界（Boundary） |
| TC-RXX | 回归（Regression） |
| TC-FXX | UI 端面功能（Page Level） |
| TC-UXXX | UI 体验审查（UX Heuristic） |
| TC-AXX | 接口（API） |
| TC-DXX | 数据验证（Data） |

---

## 回归测试执行规则（与 e2e-playwright 技能对齐）

| 触发场景 | 执行范围 |
|---------|---------|
| 单个 CR Bugfix 后验证 | 该 CR 的**全部用例**（含四、回归测试） |
| 新 CR 合入后回归 | 新 CR 全部用例 + 相关旧 CR 的"四、回归测试"章节 |
| 大版本发布前 | 所有 CR 的全部用例 |

> **说明**：通过 e2e-playwright 技能执行时，脚本层面运行整个 spec 文件（即全部用例）；"回归测试"章节是手工执行时的最小集合，自动化执行时以 spec 文件为单位。

---

## 测试报告规范

### 产出物

测试完成后，在 **test_cases.md 同目录**下生成 `e2e_test_report.md`（与 e2e-playwright 技能共用同一报告文件）。

### 触发场景

| 场景 | 操作 |
|------|------|
| 首次测试执行完成 | 创建 e2e_test_report.md |
| 回归测试 | 追加新的执行轮次段落，保留历史记录 |

### 测试结论判定规则（→ 写入报告的"测试结论"字段）

| 条件 | 结论 |
|------|------|
| P0 全部通过 + P1 通过率 ≥ 95% | ✅ 通过 |
| P0 全部通过 + P1 通过率 < 95% | ⚠️ 有风险（列出未通过项） |
| 任何 P0 失败 | ❌ 不通过 |
