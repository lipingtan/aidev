{
  "enabled": true,
  "name": "Task 全部完成后触发三角色验收",
  "description": "当 tasks.md 完成率达到 100% 时，自动触发三角色（业务专家 + 产品经理 + 架构师）全面验收，覆盖业务完整性、前后端对齐、代码质量等维度。",
  "version": "2",
  "when": {
    "type": "postToolUse",
    "toolTypes": [
      "write"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "检查：如果刚写入的文件是 tasks.md 且包含 100% 完成标记（如「完成率 | N/N (100%)」），则必须立即执行以下三角色验收流程。如果不是 tasks.md 或未达到 100%，则忽略。\n\n## 验收清单（三角色视角）\n\n### 角色1 业务专家验收\n- [ ] 需求覆盖度：requirements.md 中每个 FR-N 的验收标准是否都有代码实现覆盖\n- [ ] 多租户隔离：新增表/接口的租户隔离规则是否正确（平台级不过滤 / 业务级自动过滤）\n- [ ] 异常场景：所有定义的降级/兜底/错误场景是否都有代码实现\n- [ ] 权限边界：CRUD 权限是否正确限制到指定角色\n\n### 角色2 产品经理验收\n- [ ] 前后端字段对齐（5.2.1 全部 17 项）：打开前端 Vue 文件 + API 模块，逐条核对后端 JSON tag\n- [ ] 表单组件类型：设计说「下拉选择器」→ 前端必须是 el-select，不得是 el-input 手填 ID\n- [ ] 菜单图标：后端 seed 中的 icon 名是否在前端全局注册范围内\n- [ ] 空状态/加载态/错误提示：列表空态有 el-empty、操作中有 loading、错误有 ElMessage\n- [ ] 乐观锁 version：编辑表单是否隐藏携带 version，接口返回是否包含 version\n- [ ] 分页参数：前后端 page/page_size 命名一致\n- [ ] 雪花 ID 精度：int64 字段前后端均用 string 类型\n- [ ] 删除确认：危险操作有二次确认弹窗，文案含操作对象标识\n\n### 角色3 架构师验收\n- [ ] go build ./... 零错误\n- [ ] go vet ./... 无警告\n- [ ] 不变行为清单（RG-N）：每条回归项是否仍然成立（手动或自动验证）\n- [ ] 缓存一致性：写操作成功后才失效缓存，失败不失效\n- [ ] 中间件顺序：公开路由不走 AuthMiddleware，admin 路由走完整链\n- [ ] 乐观锁实现：Update 的 WHERE 含 version，RowsAffected=0 时返回冲突错误\n- [ ] 软删除兼容：唯一性校验只查活跃记录（deleted_at IS NULL）\n- [ ] Repository 分层：不在 Repository 层做跨表 JOIN\n\n## 执行方式\n1. 逐条检查上述清单，打开实际代码文件验证（不凭记忆）\n2. 发现问题记录到验收报告（严重度分级：高/中/低）\n3. 高优问题当前 CR 内修复；中低优视影响决定\n4. 全部通过后输出「✅ 三角色验收通过」"
  }
}
